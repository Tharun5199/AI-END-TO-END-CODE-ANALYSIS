# Web app
Flask==3.1.3
gunicorn==23.0.0
python-dotenv==1.2.3

# Git repo cloning
GitPython==3.1.62

# RAG pipeline
langchain==1.4.2
langchain-core==1.6.3
langchain-text-splitters==1.1.2
langchain-groq==1.1.3          # free-tier LLM (Groq)
langchain-chroma==1.1.0        # vector store
chromadb==1.5.9                # also provides the local ONNX all-MiniLM-L6-v2 embedding model

# No PyTorch / sentence-transformers on purpose: embeddings run on onnxruntime
# (installed with chromadb), which keeps the app under ~512 MB RAM so it fits
# free hosting tiers.
