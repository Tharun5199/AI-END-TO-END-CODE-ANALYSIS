# Web app
Flask==3.1.3
gunicorn==23.0.0
python-dotenv==1.2.3

# Git repo cloning
GitPython==3.1.62

# RAG pipeline
langchain==1.4.2
langchain-core==1.6.3
langchain-text-splitters==1.1.2
langchain-groq==1.1.3          # free-tier LLM (Groq)
langchain-huggingface==1.2.2   # local, free embeddings
langchain-chroma==1.1.0        # vector store
chromadb==1.5.9
sentence-transformers==6.1.0
torch==2.14.0                  # pulled in by sentence-transformers; see Dockerfile for the CPU-only build

# -e . also installs this project's own `codeanalyzer` package (see setup.py)
